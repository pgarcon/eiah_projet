#include <stdio.h>
#include <time.h>
#include <unistd.h>

int main( int argc, char * argv[] ) {
    
    time_t t =time(NULL);
    printf("%s\n",ctime(&t));
    argc = pid_t wait(int *rapport) ;
    printf("%d\n",argc);
}